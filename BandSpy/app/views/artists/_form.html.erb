<%= form_tag(@artist) do %>
  <div class="field">
    <%= label_tag :follow_new_artist %><br>
    <%= text_field_tag :name, placeholder: "Artist's Name" %>
  </div>

  <div class="actions">
    <%= submit_tag "Follow" %>
  </div>
<% end %>


